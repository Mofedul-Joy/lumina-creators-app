import { ComingSoon } from "@/components/admin/ComingSoon";

export default function AdminAnalyticsPage() {
  return (
    <ComingSoon
      title="Analytics"
      blurb="Network-wide performance — views over time, top creators, cost-per-view trends, and campaign ROI in one view."
    />
  );
}
