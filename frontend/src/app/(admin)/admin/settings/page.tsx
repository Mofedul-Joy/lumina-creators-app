import { ComingSoon } from "@/components/admin/ComingSoon";

export default function AdminSettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      blurb="Workspace configuration — branding, default campaign terms, payout thresholds, integrations, and team access."
    />
  );
}
