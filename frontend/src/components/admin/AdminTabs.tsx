// Deprecated: section navigation now lives in the collapsible AdminSidebar,
// mounted by app/(admin)/admin/layout.tsx. Kept as a no-op so pages that still
// render <AdminTabs /> don't need touching.
export function AdminTabs() {
  return null;
}
